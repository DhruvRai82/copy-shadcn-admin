// This file isolates the Admin App from parent directory PostCSS configs.
// Since Admin uses Tailwind v4 (Alpha/Beta) via the Vite plugin, we leave this empty
// so it doesn't try to load the parent's Tailwind v3 config.
export default {};
